import { Typography } from '@mui/material';

const Resume = () => {

  return (
    <div style={{ marginTop: '80px', padding: '20px' }}>
      <Typography variant='h1' color='primary'>Resumen del producto</Typography>
    </div>
  );
};

export default Resume;